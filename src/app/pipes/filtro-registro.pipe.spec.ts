import { FiltroRegistroPipe } from './filtro-registro.pipe';

describe('FiltroRegistroPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroRegistroPipe();
    expect(pipe).toBeTruthy();
  });
});
